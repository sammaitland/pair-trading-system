# Research Protocol

TODO(sam): This document describes the protocol for evaluating new signals,
filters, and parameter changes before deployment.

## Signal Evaluation Framework

TODO(sam): How a candidate signal is evaluated (backtest methodology,
walk-forward requirements, statistical thresholds).

## Deployment Criteria

TODO(sam): What a signal must demonstrate before it can be integrated into
the live pipeline.

## Rejected Signal Archive

TODO(sam): How rejected signals are documented to avoid re-evaluation.

## Parameter Change Protocol

TODO(sam): How parameter changes (thresholds, weights, lookbacks) are
validated before deployment.
