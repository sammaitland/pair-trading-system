# Calibration pipeline: universe determination, beta estimation, pair generation,
# parameter extraction, metrics calculation, optimisation
#
# Proprietary components (pair generation, optimisation) are represented by
# interface + reference implementation pairs, following the same pattern as
# signals/. See pair_generator_interface.py and optimizer_interface.py.
